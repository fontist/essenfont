---
# Remap table: NotoSerifTaiYo → Unicode Tai Yo
#
# STATUS: NOT NEEDED — no remap required.
#
# Earlier analysis incorrectly identified Tai Yo as U+16E40..U+16E9F
# (which is actually Medefaidrin, Unicode 11.0). The actual Unicode
# 17.0 assignment for Tai Yo is **U+1E6C0..U+1E6FF**.
#
# NotoSerifTaiYo's cmap covers U+1E6C0..U+1E6FF directly:
#   - 64/64 codepoints present
#   - No PUA remapping needed
#   - Donor can be used as-is (manifest entry has no codepoint_remap
#     field; just `covers: [Tai_Yo]`)
#
# This file is kept for documentation; the `mappings: []` empty list
# means "no remap". The build script tolerates this (skips the donor
# only if mappings is non-empty AND target is wrong; here the donor's
# cmap is already at the correct Unicode positions).
#
# Source font: references/input-fonts/NotoSerifTaiYo-Regular.ttf
# Chart: https://www.unicode.org/charts/PDF/U16E40.pdf (Medefaidrin, NOT Tai Yo)
# Actual Tai Yo chart: https://www.unicode.org/charts/PDF/U1E6C0.pdf

donor: noto-serif-tai-yo
donor_file: references/input-fonts/NotoSerifTaiYo-Regular.ttf
donor_sha256: "980924d6e1935c8d82323692fdc0bb0f2a0d85ac09efa422aa27e9f9460ee860"

target_block: Tai_Yo
target_range: [0x1E6C0, 0x1E6FF]
target_chart_url: https://www.unicode.org/charts/PDF/U1E6C0.pdf

derivation: not-needed-direct-coverage
derived_at: "2026-06-30"
derived_by: "essenfont issue #3 close-out"

mappings: []
